pub mod constants {
  pub const MILLISECOND: usize = 1;
  pub const SECOND: usize = 1000 * MILLISECOND;
  pub const MINUTE: usize = 60 * SECOND;
  pub const HOUR: usize = 60 * MINUTE;
  pub const DAY: usize = 24 * HOUR;
  pub const WEEK: usize = 7 * DAY;
}
